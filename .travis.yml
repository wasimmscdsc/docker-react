language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t wasimmscdsc/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true wasimmscdsc/docker-react npm run test